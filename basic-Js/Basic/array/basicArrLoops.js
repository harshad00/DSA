/**
 * !Prints each element of the array using a traditional for loop.
 * @param {number[]} arr - The array of numbers to print.
 */
// function printArrayElements(arr) {
//     // TODO: Loop through array with index
//     for (let i = 0; i < arr.length; i++) {
//         console.log(arr[i]);
//     }
// }

// printArrayElements([10, 20, 30, 40]);

/**
 * !Filters and prints even numbers from an array.
 * @param {number[]} arr - The array to filter.
 */
// function printEvenNumbers(arr) {
//     let evenNumbers = [];

//     // TODO: Use for...of loop to find even numbers
//     for (let num of arr) {
//         if (num % 2 === 0) {
//             evenNumbers.push(num);
//         }
//     }

//     console.log("Even numbers:", evenNumbers);
// }

// printEvenNumbers([1, 2, 3, 4, 5, 6]);

/**
 * !Filters and prints even numbers from an array.
 * @param {number[]} arr - The array to filter.
 */

// function printEvenNumbers(arr) {
//     let evenNumbers = [];

    // TODO: Use for...of loop to find even numbers
//     for (let num of arr) {
//         if (num % 2 === 0) {
//             evenNumbers.push(num);
//         }
//     }

//     console.log("Even numbers:", evenNumbers);
// }

// printEvenNumbers([1, 2, 3, 4, 5, 6]);

/**
 * !Prints each character from a character array using for...of.
 * @param {string[]} charArray - The array of characters.
 */
// function printCharacters(charArray) {
//     // TODO: Loop and print characters
//     for (let char of charArray) {
//         console.log(char);
//     }
// }

// printCharacters(["a", "b", "c"]);

